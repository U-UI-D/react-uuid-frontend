

const AppConfig = {
  backendDev: {
    host: "localhost",
    port: "8764",
    prefix: "/api/v1",
  },

  backendProd: {
    host: "localhost",
    port: "8764",
    prefix: "/api/v1",
  }

}

export default AppConfig;
